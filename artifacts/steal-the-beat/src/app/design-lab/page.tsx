import { DesignLabClient } from "@/components/DesignLabClient";

export const metadata = {
  title: "Community Design Lab — Steal the Beat by Blayke",
  description:
    "Vote on the next Steal the Beat drop. Your choice shapes the collection.",
};

export default function DesignLabPage() {
  return <DesignLabClient />;
}
